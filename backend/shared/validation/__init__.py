"""Shared input-validation pure helpers (Property 3 / 4 / 6 / ...).

Sub-modules:
  - range_enum: `in_range` / `in_enum` (Property 4, Phase 13.4)
  - is_valid_e164 (Property 3, Phase 13.3) lives in
    `shared.validation.phone` (Phase 13.3) — re-exported separately.
"""

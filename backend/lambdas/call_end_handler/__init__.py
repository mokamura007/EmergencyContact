"""CallEndHandler Lambda — finalise one Connect outbound call (Phase 6.3).

Invoked from the terminal ``Invoke AWS Lambda function`` block of the
Outbound Contact Flow (Phase 7.1). See ``handler.py`` for the full
interface contract.
"""
